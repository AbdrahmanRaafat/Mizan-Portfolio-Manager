{
  "name": "mizan-portfolio-manager",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bash build.sh",
    "deploy": "npm run build && wrangler deploy",
    "deploy:dry": "npm run build && wrangler deploy --dry-run --outdir dist-worker"
  },
  "devDependencies": {
    "wrangler": "^4.102.0"
  }
}
